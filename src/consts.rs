pub const TILE_SIZE: u32 = 64;
pub const MAP_WIDTH: usize = 10;
pub const MAP_HEIGHT: usize = 10;

pub const WINDOW_WIDTH: u32 = (MAP_WIDTH as u32) * TILE_SIZE;
pub const WINDOW_HEIGHT: u32 = (MAP_HEIGHT as u32) * TILE_SIZE;
pub const WINDOW_TITLE: &str = "Black Diamond";